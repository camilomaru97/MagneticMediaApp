
const initialState = {
    catalogo: [],
    loading: false,
    error: null
};

export const catalogoReducer = (state = initialState, action) => {
    switch(action.type){
        
        default: return state;
    }
}